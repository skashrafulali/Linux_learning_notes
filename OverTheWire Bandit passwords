Command Line:  ssh bandit0@bandit.labs.overthewire.org -p 2220
Bandit0 password: bandit

# Bandit0 password: ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If
by using "cat readme".

# Bandit1 password: 263JGJPfgU6LtdEvgfWU1XP5yac29mFx
by using "cat ./-". Cz the file name was "-" and you cant open a file named "-" , the terminal would assume I am wanting to write a command. So we have to use ( ./ ) and then the file name "-".

# Bandit2 password: MNk8KNH3Usiio41PRUEoDFPqfxLPlSmx
by using "cat ./"--spaces in this filename--" as the file name had spaces between them and also started with "--" so the terminal was confused to look at it using cat. To eradicate "--" problem we used ( ./ ) and to eradicate spaces problem we quoted the whole filename.

 # Bandit3 password: 2WmrDFRmJIq3IPxneAaMGhap0pFhF3NJ
By using cat ./. . . Hiding from you As it was a hidden file so I used "ls -a" to view everything and then user Tab Tab to get the names correctly.

# Bandit4 password: 4oQYVPkxZOOEOO5pTW81FB8j8lxXGUQw
There were several files in the folder. But it was hard to look for files by checking each one by one. So I used "file ./*" to check for existing files in every single folder. Then files appear an I go for ASCII labeled file as ASCII is human readable. 

# Bandit5 password: HWasnPhtq9AVKe0dmk45nxy20cvUa6EG
There were so many files and so many data and so many folders.
The hint says 
• human-readable
• 1033 bytes in size
• not executable
So I go for "find" and searched for file type thing. Which is not executable, and has minimum size. "find . -type f -size 1033c ! -executable"
c=bytes

# Bandit6 password: morbNTDkSW6jIlUc0ymOdMaLnOlFVAaj
owned by user bandit7 
owned by group bandit6 
size 33 bytes

But there were many permission related issues. 
so "find / -type f -user bandit7 -group bandit6 -size 33c "
Every folder denied permission except one. So that’s the file

# Bandit7 password: dfwvzFQi4mU0wfNbFOe9RoWskMLg7eEc
as it says it was next to a word millionth. So I used "grep" to find for a line. And then used "grep "millionth*" data.txt"
I used * so that the terminal can understand that there are words after that. If millionth was at the end of the line I would used millionth$. Cz $ means end of line

# Bandit8 password: 4CKMh1JI91bUIZZPXDqGanal4xvAg0JM
told to find a word that only occurs once ,means that was occurring once.
so "sort data.txt | uniq -u" to find no repeating.
"sort data.txt | uniq -c" to find counts of repeating

# Bandit9 password:
